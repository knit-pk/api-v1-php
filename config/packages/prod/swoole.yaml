parameters:
    env(HOST): localhost
    env(PORT): 9501

swoole:
    server:
        port: '%env(int:PORT)%'
        host: '%env(HOST)%'
        use_advanced_static_handler: true
        settings:
            worker_count: 9
            reactor_count: 3
            serve_static_files: false
            public_dir: '%kernel.project_dir%/public'
